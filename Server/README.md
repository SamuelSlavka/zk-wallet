# Flask backend
REST API backend

#### Setup:
    make init
  
#### Starting in development mode:
    make setup
  
#### Starting the whole system:
    make start


#### Execution:
   http://localhost:5000/api/<query>
